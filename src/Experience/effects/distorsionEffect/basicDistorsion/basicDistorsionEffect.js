import fragmentShader from './shaders/fragment.glsl';
import vertexShader from './shaders/vertex.glsl';

export const shadersOptions = {
  vertex: vertexShader,
  fragment: fragmentShader,
};

export default shadersOptions;